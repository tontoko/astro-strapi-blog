query aaa {
  posts {
    data {
      attributes {
        title
        content
        slug
      }
    }
  }
}

query bbb($slug: String) {
  posts(filters: { slug: { eq: $slug } }) {
    data {
      attributes {
        title
        content
        slug
      }
    }
  }
}

query queryPostBySlug($slug: String) {
  posts(filters: { slug: { eq: $slug } }) {
    data {
      attributes {
        title
        content
        slug
        seo {
          ...Seo
        }
      }
    }
  }
}

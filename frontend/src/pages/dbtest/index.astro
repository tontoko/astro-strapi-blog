---
export const prerender = false

import Layout from '../../layouts/Layout.astro';
import { CommentForm } from '../../components/comments/CommentForm';

const origin = Astro.url.origin

---

<Layout title="Todo">
  <CommentForm post_id={1} origin={origin} />
</Layout>